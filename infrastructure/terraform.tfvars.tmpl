aws_region="${AWS_REGION}"
prefix="${PREFIX}"
vpc_id="${VPC_ID}"
backend_vpc_id="${BACKEND_VPC_ID}"
subnet_ids=$SUBNET_IDS
subnet_tagname="${SUBNET_TAGNAME}"
iam_policy_permissions_boundary_name="${PERMISSIONS_BOUNDARY_POLICY_NAME:-null}"
stage="${STAGE:-dev}"
assume_role_arns=${ASSUME_ROLE_ARNS:-"[]"}
cognito_app_secret="${VEDA_PROGRAMMATIC_CLIENT_SECRET_ID}"
workflows_client_secret="${VEDA_WORKFLOWS_CLIENT_SECRET_ID}"
stac_ingestor_api_url="${VEDA_STAC_INGESTOR_API_URL}"
data_access_role_arn="${VEDA_DATA_ACCESS_ROLE_ARN}"
raster_url="${VEDA_RASTER_URL}"
stac_url="${VEDA_STAC_URL}"
vector_secret_name="${VECTOR_SECRET_NAME}"
vector_security_group="${VECTOR_SECURITY_GROUP}"
vector_vpc="${VECTOR_VPC:-null}"
workflow_root_path="${WORKFLOW_ROOT_PATH:-/api/workflows}"
cloudfront_id="${VEDA_CLOUDFRONT_ID}"
cognito_domain="${VEDA_COGNITO_DOMAIN}"
client_id="${VEDA_CLIENT_ID}"
userpool_id="${VEDA_USERPOOL_ID}"
