aws_region="${AWS_REGION}"
prefix="${PREFIX}"
vpc_id="${VPC_ID}"
subnet_tagname="${SUBNET_TAGNAME}"
iam_policy_permissions_boundary_name="${PERMISSIONS_BOUNDARY_POLICY_NAME:-null}"
stage="${STAGE:-dev}"
assume_role_arns=${ASSUME_ROLE_ARNS:-"[]"}
veda_cognito_app_secret="${VEDA_COGNITO_APP_SECRET:-appsec}"
veda_stac_ingestor_api_url="${VEDA_STAC_INGESTOR_API_URL}"
vector_secret_name="${STAC_DB_SECRET_NAME}"
vector_security_group="${STAC_DB_SECURITY_GROUP_ID}"
