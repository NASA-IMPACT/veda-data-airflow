aws_region="${AWS_REGION}"
prefix="${PREFIX}"
vpc_id="${VPC_ID}"
subnet_tagname="${SUBNET_TAGNAME}"
iam_policy_permissions_boundary_name="${PERMISSIONS_BOUNDARY_POLICY_NAME:-null}"
stage="${STAGE:-dev}"
assume_role_arns=$ASSUME_ROLE_ARNS
veda_cognito_app_secret="${VEDA_COGNITO_APP_SECRET}"
veda_stac_ingestor_api_url="${VEDA_STAC_INGESTOR_API_URL}"
veda_data_access_role_arn="${VEDA_DATA_ACCESS_ROLE_ARN}"
veda_raster_url="${VEDA_RASTER_URL}"
veda_stac_url="${VEDA_STAC_URL}"
vector_secret_name="${VECTOR_SECRET_NAME}"
vector_security_group="${VECTOR_SECURITY_GROUP}"
vector_vpc="${VECTOR_VPC}"
workflow_root_path="${WORKFLOW_ROOT_PATH}"
cloudfront_id="${CLOUDFRONT_ID}"
